# РекламаСтрой Designer — v2.1 Stable Cloudflare

Стабильная версия для Cloudflare Pages.

Настройки Cloudflare:

- Framework preset: React (Vite)
- Build command: `npm run build`
- Build output directory: `dist`

Важно: в репозиторий не загружать `package-lock.json` и `node_modules`.
